#include "main.h"
/**
 *_isalpha - main function
 *@c: ASCII character
 *
 *Return: 1 (if true)
 *
 */
int _isalpha(int c)
{
if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z')
{
return (1);
}
return (0);
}
